services:
  app:
    image: mysociety/parl_register_experiment:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.dev
    working_dir: /workspaces/parl_register_experiment
    volumes:
      - ./:/workspaces/parl_register_experiment/
